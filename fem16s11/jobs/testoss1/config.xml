<?xml version="1.0" encoding="UTF-8"?><flow-definition>
    <actions/>
    <description>ENM Cloud Native Helm Chart Library Build pipeline autogenerated using JobDSL - all manual changes will be overwritten!</description>
    <keepDependencies>false</keepDependencies>
    <properties/>
    <triggers/>
    <logRotator>
        <daysToKeep>-1</daysToKeep>
        <numToKeep>30</numToKeep>
        <artifactDaysToKeep>-1</artifactDaysToKeep>
        <artifactNumToKeep>-1</artifactNumToKeep>
    </logRotator>
    <definition class="org.jenkinsci.plugins.workflow.cps.CpsFlowDefinition">
        <script>
def sg_gerrit_repo= "${repo_format}${pipeline_name}"

pipeline {
      agent {
        label 'Cloud-Native-Pointfix'
      }
      stages {
          stage('Checkout Cloud-Native SG Git Repository') {
            steps {
                git branch: env.BRANCH,
                        url: "${GERRIT_MIRROR}/${sg_gerrit_repo}"
                sh "git remote set-url origin --push ${GERRIT_CENTRAL}/${sg_gerrit_repo}"
            }
          }
      }
}
</script>
        <sandbox>true</sandbox>
    </definition>
</flow-definition>